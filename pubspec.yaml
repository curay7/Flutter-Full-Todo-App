name: lonetodo
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  hive_flutter:
  hive:
  cupertino_icons: ^1.0.2
  get: 4.6.1
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner:
  hive_generator:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
